// 3º JS - index.js

// Nesse JS fica o Array com a lista dos Produtos (listProdutos) e função C do CRUD ou seja:
// CRUD = Create (inserir) Retreave (listar ou recuperar) Update (alterar) Delete (deletar)
var listProdutos = [];

function AdicionarProduto(produto) {
    listProdutos.push(produto);
}
